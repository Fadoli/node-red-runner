/**
 * Constants for file system operations
 */
module.exports = {
    // Threshold in bytes for automatic compression (1MB)
    COMPRESSION_THRESHOLD: 1024 * 1024,
    // File extensions
    BACKUP_EXT: '.backup',
    TMP_EXT: '.tmp'
};
