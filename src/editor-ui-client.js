const SVG = 'http://www.w3.org/2000/svg';
const canvas = document.getElementById('canvas');
const palette = document.getElementById('palette');
const inspector = document.getElementById('inspector');
const statusEl = document.getElementById('status');
const metrics = document.getElementById('metrics');

const state = {
    flow: { flows: [], rev: '0' }, types: [], tabId: null, selectedId: null, selectedIds: new Set(),
    history: [], redo: [], baseline: new Map(), statuses: new Map(), connectDrag: null, marquee: null, selectedWire: null,
    zoom: 1, nodes: new Map(), wires: new Map(), drag: null,
};
const NODE_WIDTH = 140;
const locked = new Set(['id', 'type', 'z', 'wires', 'x', 'y', 'l', 'w', '_credentialId']);
const clone = value => JSON.parse(JSON.stringify(value));
const tabs = () => {
    const real = state.flow.flows.filter(node => node.type === 'tab');
    if (real.length) return real;
    const result = [], seen = new Set();
    for (const node of state.flow.flows) if (node.z && !seen.has(node.z)) {
        seen.add(node.z); result.push({ id: node.z, label: node.z });
    }
    return result;
};
const activeTab = () => tabs().find(tab => tab.id === state.tabId) || tabs()[0];
const nodes = () => state.flow.flows.filter(node => node.type !== 'tab' && node.type !== 'subflow' && node.wires !== undefined && (!activeTab() || node.z === activeTab().id));
const selected = () => state.flow.flows.find(node => node.id === state.selectedId) || null;
const selectedNodes = () => state.flow.flows.filter(node => state.selectedIds.has(node.id));
const modified = node => state.baseline.size > 0 && (!state.baseline.has(node.id) || JSON.stringify(node) !== state.baseline.get(node.id));
function setStatus(text, good) { statusEl.textContent = text; statusEl.style.color = good ? '#55d187' : '#8b9bad'; }
function readableText(color) { const match = /^#?([0-9a-f]{6})$/i.exec(color || ''); if (!match) return '#f2f6fb'; const value = parseInt(match[1], 16); const r = value >> 16 & 255, g = value >> 8 & 255, b = value & 255; return (r * 299 + g * 587 + b * 114) / 1000 > 155 ? '#182331' : '#f2f6fb'; }
function saveHistory() { state.history.push(clone(state.flow)); state.redo.length = 0; if (state.history.length > 30) state.history.shift(); }
function restore(snapshot) { state.flow = snapshot; state.selectedId = null; state.selectedIds.clear(); renderTabs(); renderInspector(); renderNodes(); updateMetrics(); }
function undo() { const previous = state.history.pop(); if (!previous) return; state.redo.push(clone(state.flow)); restore(previous); setStatus('Undo'); }
function redo() { const next = state.redo.pop(); if (!next) return; state.history.push(clone(state.flow)); restore(next); setStatus('Redo'); }
function snap(value) { return Math.max(0, Math.round(value / 24) * 24); }
function point(event) { const box = canvas.getBoundingClientRect(); return { x: (event.clientX - box.left) / state.zoom, y: (event.clientY - box.top) / state.zoom }; }
function portY(index, count) { return count <= 1 ? 29 : 12 + index * (34 / (count - 1)); }
function nodePoint(node, side, index = 0) { const count = side === 'out' ? Number((state.types.find(item => item.type === node.type) || {}).outputs || 1) : Number((state.types.find(item => item.type === node.type) || {}).inputs || 1); return { x: Number(node.x) + (side === 'out' ? NODE_WIDTH : 0), y: Number(node.y) + portY(index, count) }; }
function wirePath(from, to, outputIndex = 0) { const a = nodePoint(from, 'out', outputIndex), b = nodePoint(to, 'in', 0); return 'M '+a.x+' '+a.y+' C '+(a.x + 70)+' '+a.y+', '+(b.x - 70)+' '+b.y+', '+b.x+' '+b.y; }
function ensurePreview() { let preview = document.getElementById('wire-preview'); if (!preview) { preview = document.createElementNS(SVG, 'path'); preview.id = 'wire-preview'; preview.setAttribute('class', 'wire hot'); preview.style.pointerEvents = 'none'; canvas.append(preview); } return preview; }
function updatePreview(event) { const from = state.connectDrag && state.connectDrag.from; if (!from) return; const a = nodePoint(from, 'out'), cursor = point(event); const preview = ensurePreview(); preview.setAttribute('d', 'M '+a.x+' '+a.y+' C '+(a.x + 70)+' '+a.y+', '+(cursor.x - 70)+' '+cursor.y+', '+cursor.x+' '+cursor.y); }
function finishConnection(event) { const drag = state.connectDrag; state.connectDrag = null; const preview = document.getElementById('wire-preview'); if (preview) preview.remove(); const element = document.elementFromPoint(event.clientX, event.clientY); const target = element && element.closest ? element.closest('.node') : null; const to = target && state.flow.flows.find(node => node.id === target.dataset.id); if (to && drag && to.id !== drag.from.id) connect(drag.from, to); else renderNodes(); }
function updateMetrics() { metrics.textContent = nodes().length+' nodes · '+nodes().filter(modified).length+' modified'; }
function drawWires() {
    let layer = document.getElementById('wire-layer'); if (!layer) { layer = document.createElementNS(SVG, 'g'); layer.id = 'wire-layer'; canvas.append(layer); }
    layer.replaceChildren(); state.wires.clear(); const visible = new Map(nodes().map(node => [node.id, node]));
    for (const from of nodes()) {
        const outputs = from.wires || [];
        for (let outputIndex = 0; outputIndex < outputs.length; outputIndex++) for (const id of outputs[outputIndex] || []) { const to = visible.get(id) || state.flow.flows.find(node => node.id === id); if (!to || to.z !== from.z) continue;
            const key = from.id+'>'+to.id+'>'+outputIndex; const path = document.createElementNS(SVG, 'path'); path.setAttribute('class', 'wire'+(state.selectedWire === key ? ' selected' : '')); path.dataset.from = from.id; path.dataset.to = to.id; path.dataset.output = outputIndex; path.dataset.key = key; path.setAttribute('d', wirePath(from, to, outputIndex)); path.addEventListener('click', event => { event.stopPropagation(); state.selectedWire = key; renderNodes(); }); layer.append(path); state.wires.set(key, path);
        }
        for (const id of from.links || []) { const to = visible.get(id) || state.flow.flows.find(node => node.id === id); if (!to || to.z !== from.z) continue; const key = from.id+'>'+to.id+'>link'; const path = document.createElementNS(SVG, 'path'); path.setAttribute('class', 'wire'+(state.selectedWire === key ? ' selected' : '')); path.dataset.from = from.id; path.dataset.to = to.id; path.dataset.output = 0; path.dataset.key = key; path.setAttribute('d', wirePath(from, to)); path.addEventListener('click', event => { event.stopPropagation(); state.selectedWire = key; renderNodes(); }); layer.append(path); state.wires.set(key, path); }
    }
}
function updateWirePositions() { for (const path of state.wires.values()) { const from = state.flow.flows.find(node => node.id === path.dataset.from), to = state.flow.flows.find(node => node.id === path.dataset.to); if (from && to) path.setAttribute('d', wirePath(from, to, Number(path.dataset.output || 0))); } }
function beginMarquee(event) { const start = point(event); const box = document.createElementNS(SVG, 'rect'); box.setAttribute('class', 'selection-box'); box.setAttribute('x', start.x); box.setAttribute('y', start.y); box.setAttribute('width', 0); box.setAttribute('height', 0); canvas.append(box); state.marquee = { start, box, pointerId: event.pointerId }; canvas.setPointerCapture(event.pointerId); }
function updateMarquee(event) { if (!state.marquee) return; const current = point(event), x = Math.min(state.marquee.start.x, current.x), y = Math.min(state.marquee.start.y, current.y), width = Math.abs(current.x - state.marquee.start.x), height = Math.abs(current.y - state.marquee.start.y); state.marquee.box.setAttribute('x', x); state.marquee.box.setAttribute('y', y); state.marquee.box.setAttribute('width', width); state.marquee.box.setAttribute('height', height); }
function finishMarquee(event) { if (!state.marquee) return; const current = point(event), x1 = Math.min(state.marquee.start.x, current.x), y1 = Math.min(state.marquee.start.y, current.y), x2 = Math.max(state.marquee.start.x, current.x), y2 = Math.max(state.marquee.start.y, current.y); state.marquee.box.remove(); state.marquee = null; state.selectedIds.clear(); for (const node of nodes()) if (Number(node.x) < x2 && Number(node.x) + NODE_WIDTH > x1 && Number(node.y) < y2 && Number(node.y) + 58 > y1) state.selectedIds.add(node.id); state.selectedId = state.selectedIds.values().next().value || null; renderInspector(); renderNodes(); updateMetrics(); }
function selectNode(node, additive = false) { if (!node) { state.selectedId = null; state.selectedIds.clear(); } else if (additive) { if (state.selectedIds.has(node.id)) state.selectedIds.delete(node.id); else state.selectedIds.add(node.id); state.selectedId = state.selectedIds.has(node.id) ? node.id : (state.selectedIds.values().next().value || null); } else { state.selectedId = node.id; state.selectedIds = new Set([node.id]); } renderInspector(); renderNodes(); }
function renderTabs() { const holder = document.getElementById('tabs'); holder.replaceChildren(); for (const tab of tabs()) { const button = document.createElement('button'); button.className = 'tab'+(activeTab() === tab ? ' active' : ''); button.textContent = tab.label || tab.name || tab.id; button.onclick = () => { state.tabId = tab.id; state.selectedId = null; renderTabs(); renderInspector(); renderNodes(); updateMetrics(); }; holder.append(button); } }
function makePort(x) { const port = document.createElementNS(SVG, 'circle'); port.setAttribute('class', 'port'); port.setAttribute('cx', x); port.setAttribute('cy', 29); port.setAttribute('r', 6); return port; }
function renderNodes() {
    const visibleIds = new Set(nodes().map(node => node.id)); for (const id of state.selectedIds) if (!visibleIds.has(id)) state.selectedIds.delete(id); if (state.selectedId && !visibleIds.has(state.selectedId)) state.selectedId = state.selectedIds.values().next().value || null;
    let layer = document.getElementById('node-layer'); if (!layer) { layer = document.createElementNS(SVG, 'g'); layer.id = 'node-layer'; canvas.append(layer); }
    layer.replaceChildren(); state.nodes.clear();
    for (const node of nodes()) {
        const group = document.createElementNS(SVG, 'g'); group.dataset.id = node.id; group.setAttribute('class', 'node'+(state.selectedIds.has(node.id) ? ' selected' : '')+(modified(node) ? ' modified' : '')); group.setAttribute('transform', 'translate('+node.x+','+node.y+')');
        const definition = state.types.find(item => item.type === node.type) || {}; const rect = document.createElementNS(SVG, 'rect'); rect.setAttribute('width', NODE_WIDTH); rect.setAttribute('height', 58); rect.setAttribute('rx', 8); if (definition.color) rect.style.fill = definition.color; const foreground = readableText(definition.color);
        const title = document.createElementNS(SVG, 'text'); title.setAttribute('class', 'title'); title.style.fill = foreground; title.setAttribute('x', 14); title.setAttribute('y', 24); title.textContent = node.name || node.type || node.id;
        const type = document.createElementNS(SVG, 'text'); type.setAttribute('class', 'type'); type.style.fill = foreground; type.style.opacity = '0.72'; type.setAttribute('x', 14); type.setAttribute('y', 43); type.textContent = node.type || '';
        const nodeStatus = document.createElementNS(SVG, 'text'); nodeStatus.setAttribute('class', 'node-status'); nodeStatus.style.fill = foreground; nodeStatus.setAttribute('x', 14); nodeStatus.setAttribute('y', 72); nodeStatus.textContent = state.statuses.get(node.id) || '';
        const inputs = Math.max(0, Number(definition.inputs == null ? 1 : definition.inputs)); const outputs = Math.max(0, Number(definition.outputs == null ? 1 : definition.outputs)); for (let port = 0; port < Math.max(inputs, outputs); port++) { if (port < inputs) { const inputPort = makePort(0); inputPort.setAttribute('cy', portY(port, inputs)); group.append(inputPort); } if (port < outputs) { const outputPort = makePort(NODE_WIDTH); outputPort.setAttribute('cy', portY(port, outputs)); group.append(outputPort); } } group.prepend(rect, title, type, nodeStatus);
        group.addEventListener('pointerdown', event => {
            event.preventDefault(); event.stopPropagation(); if (event.shiftKey) { if (state.selectedIds.has(node.id)) state.selectedIds.delete(node.id); else state.selectedIds.add(node.id); state.selectedId = state.selectedIds.has(node.id) ? node.id : (state.selectedIds.values().next().value || null); } else if (!state.selectedIds.has(node.id)) { state.selectedId = node.id; state.selectedIds = new Set([node.id]); } renderInspector();
            if (event.target.classList && event.target.classList.contains('port') && Number(event.target.getAttribute('cx')) > NODE_WIDTH / 2) { state.connectDrag = { from: node, pointerId: event.pointerId }; group.setPointerCapture(event.pointerId); ensurePreview(); return; }
            const cursor = point(event); state.drag = { node, group, items: selectedNodes().map(item => ({ node: item, dx: cursor.x - Number(item.x), dy: cursor.y - Number(item.y) })), dx: cursor.x - Number(node.x), dy: cursor.y - Number(node.y), pointerId: event.pointerId }; group.setPointerCapture(event.pointerId); saveHistory();
        });
        group.addEventListener('pointermove', event => { if (state.connectDrag && state.connectDrag.from === node) { updatePreview(event); return; } if (!state.drag || state.drag.node !== node) return; const cursor = point(event); for (const item of state.drag.items) { item.node.x = snap(cursor.x - item.dx); item.node.y = snap(cursor.y - item.dy); const itemGroup = state.nodes.get(item.node.id); if (itemGroup) itemGroup.setAttribute('transform', 'translate('+item.node.x+','+item.node.y+')'); } updateWirePositions(); updateMetrics(); });
        group.addEventListener('pointerup', event => { if (state.connectDrag && state.connectDrag.from === node) { finishConnection(event); return; } if (!state.drag || state.drag.node !== node) return; state.drag = null; if (group.hasPointerCapture(event.pointerId)) group.releasePointerCapture(event.pointerId); renderNodes(); drawWires(); });
        group.addEventListener('click', event => { event.stopPropagation(); });
        layer.append(group); state.nodes.set(node.id, group);
    }
    drawWires();
}
function renderInspector() {
    const node = selected(); if (state.selectedIds.size > 1) { inspector.innerHTML = '<div class="empty">'+state.selectedIds.size+' nodes selected.<br>Hold Shift to adjust the selection.</div>'; return; } if (!node) { inspector.innerHTML = '<div class="empty">Select a node to edit its properties.</div>'; return; }
    inspector.replaceChildren(); const heading = document.createElement('div'); heading.className = 'field'; heading.innerHTML = '<strong>'+String(node.name || node.type || node.id)+'</strong>'; inspector.append(heading);
    for (const key in node) { if (locked.has(key) || key[0] === '_') continue; const field = document.createElement('div'); field.className = 'field'; const label = document.createElement('label'); label.textContent = key; field.append(label); const original = node[key]; let input;
        if (typeof original === 'boolean') { input = document.createElement('input'); input.type = 'checkbox'; input.checked = original; }
        else if (original && typeof original === 'object') { input = document.createElement('textarea'); input.value = JSON.stringify(original, null, 2); }
        else { input = document.createElement('input'); input.value = original == null ? '' : String(original); }
        input.addEventListener('input', () => { try { saveHistory(); if (input.type === 'checkbox') node[key] = input.checked; else if (original && typeof original === 'object') node[key] = JSON.parse(input.value); else if (typeof original === 'number') node[key] = Number(input.value); else node[key] = input.value; updateMetrics(); renderNodes(); setStatus('Local changes'); } catch { setStatus('Invalid '+key); } }); field.append(input); inspector.append(field);
    }
}
function connect(from, to) { saveHistory(); if (!from.wires) from.wires = [[]]; if (!from.wires[0]) from.wires[0] = []; if (!from.wires[0].includes(to.id)) from.wires[0].push(to.id); renderNodes(); setStatus('Wire added'); }
function renderPalette() { const query = document.getElementById('search').value.toLowerCase(); palette.replaceChildren(); for (const definition of state.types) { if (query && !definition.type.toLowerCase().includes(query)) continue; const button = document.createElement('button'); button.textContent = definition.type; button.onclick = () => { saveHistory(); const node = { id: 'ui-'+Math.random().toString(36).slice(2, 9), type: definition.type, x: 120, y: 120, wires: [[]] }; if (activeTab()) node.z = activeTab().id; state.flow.flows.push(node); selectNode(node); renderNodes(); updateMetrics(); }; palette.append(button); } }
async function deploy() { const before = await (await fetch('/api/editor/snapshot')).json(), old = new Map(before.flows.map(node => [node.id, node])), next = new Map(state.flow.flows.map(node => [node.id, node])), changes = []; for (const node of state.flow.flows) { const previous = old.get(node.id); if (!previous) { changes.push({ op: 'add-node', node }); continue; } const set = {}; for (const key in node) if (key !== 'id' && key !== 'wires' && JSON.stringify(node[key]) !== JSON.stringify(previous[key])) set[key] = node[key]; if (Object.keys(set).length) changes.push({ op: 'update-node', id: node.id, set }); if (JSON.stringify(node.wires || []) !== JSON.stringify(previous.wires || [])) changes.push({ op: 'replace-wires', id: node.id, wires: node.wires || [] }); } for (const node of before.flows) if (!next.has(node.id)) changes.push({ op: 'remove-node', id: node.id }); if (!changes.length) { setStatus('No changes', true); return; } const response = await fetch('/api/editor/deploy', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ baseRev: before.rev, changes }) }); if (!response.ok) { setStatus((await response.json()).error || 'Deploy failed'); return; } setStatus('Deployed', true); load(); }
async function load() { state.flow = await (await fetch('/api/editor/snapshot')).json(); state.types = await (await fetch('/api/editor/node-types')).json(); state.tabId = tabs()[0] && tabs()[0].id; state.baseline = new Map(state.flow.flows.map(node => [node.id, JSON.stringify(node)])); renderTabs(); renderPalette(); renderInspector(); renderNodes(); updateMetrics(); setStatus('Ready · revision '+state.flow.rev, true); }
document.getElementById('deploy').onclick = deploy; document.getElementById('undo').onclick = () => { const previous = state.history.pop(); if (!previous) return; state.flow = previous; state.selectedId = null; state.selectedIds.clear(); state.selectedWire = null; renderTabs(); renderInspector(); renderNodes(); updateMetrics(); }; document.getElementById('wire').onclick = () => { state.wireMode = !state.wireMode; state.wireFrom = null; setStatus(state.wireMode ? 'Wire mode · select source then target' : 'Ready'); }; document.getElementById('add').onclick = () => { const first = palette.querySelector('button'); if (first) first.click(); }; document.getElementById('search').oninput = renderPalette; canvas.addEventListener('pointerdown', event => { const nodeTarget = event.target.closest && event.target.closest('.node'); const wireTarget = event.target.classList && event.target.classList.contains('wire'); if (!nodeTarget && !wireTarget && !event.target.classList.contains('port')) { state.selectedId = null; state.selectedIds.clear(); state.selectedWire = null; renderInspector(); beginMarquee(event); } }); canvas.addEventListener('pointermove', updateMarquee); canvas.addEventListener('pointerup', finishMarquee); canvas.addEventListener('wheel', event => { event.preventDefault(); state.zoom = Math.max(.5, Math.min(2, state.zoom * (event.deltaY < 0 ? 1.1 : .9))); canvas.style.transform = 'scale('+state.zoom+')'; }, { passive: false }); document.addEventListener('keydown', event => { if (event.key === 'w' || event.key === 'W') document.getElementById('wire').click(); const node = selected(); if (event.key === 'Delete' && node) { saveHistory(); state.flow.flows = state.flow.flows.filter(item => item !== node); state.selectedId = null; state.selectedIds.clear(); renderInspector(); renderNodes(); updateMetrics(); } }); load().catch(error => setStatus('Load failed: '+error.message));
document.getElementById('undo').onclick = undo;
document.addEventListener('keydown', event => { if (!(event.ctrlKey || event.metaKey)) return; const key = event.key.toLowerCase(); if (key === 'z') { event.preventDefault(); event.shiftKey ? redo() : undo(); } else if (key === 'y') { event.preventDefault(); redo(); } });
document.addEventListener('pointermove', event => { const drag = state.drag; if (!drag || drag.pointerId !== event.pointerId) return; const cursor = point(event); for (const item of drag.items) { item.node.x = snap(cursor.x - item.dx); item.node.y = snap(cursor.y - item.dy); const group = state.nodes.get(item.node.id); if (group) group.setAttribute('transform', 'translate('+item.node.x+','+item.node.y+')'); } updateWirePositions(); updateMetrics(); });
document.addEventListener('pointerup', event => { if (!state.drag || state.drag.pointerId !== event.pointerId) return; state.drag = null; renderNodes(); drawWires(); });
const debugPanel = document.getElementById('debug-panel');
const debugLog = document.getElementById('debug-log');
const debugCount = document.getElementById('debug-count');
let debugPaused = false, debugEvents = 0;
const debugButton = document.getElementById('debug-toggle') || (() => { const button = document.createElement('button'); button.id = 'debug-toggle'; button.textContent = '▣ Debug'; document.querySelector('.top').insertBefore(button, document.getElementById('deploy')); return button; })();
function appendDebug(value, kind) { if (debugPaused) return; debugEvents += 1; debugCount.textContent = debugEvents+' events'; const line = document.createElement('div'); line.className = 'debug-line '+(kind === 'error' ? 'debug-error' : kind === 'status' ? 'debug-status' : ''); const stamp = new Date().toLocaleTimeString(); line.textContent = '['+stamp+'] '+(typeof value === 'string' ? value : JSON.stringify(value)); debugLog.append(line); debugLog.scrollTop = debugLog.scrollHeight; }
debugButton.onclick = () => debugPanel.classList.toggle('open');
document.getElementById('debug-pause').onclick = event => { debugPaused = !debugPaused; event.currentTarget.textContent = debugPaused ? 'Resume' : 'Pause'; };
document.getElementById('debug-clear').onclick = () => { debugLog.replaceChildren(); debugEvents = 0; debugCount.textContent = '0 events'; };
try { const stream = new EventSource('/api/editor/events'); stream.addEventListener('status', event => { appendDebug(event.data, 'status'); try { const value = JSON.parse(event.data), source = value.source || {}, status = value.status || {}; if (source.id) { state.statuses.set(source.id, status.text || status.fill || status.shape || String(status)); renderNodes(); } } catch {} }); stream.addEventListener('deploy', event => appendDebug('deploy '+event.data, 'status')); stream.addEventListener('comms', event => appendDebug(event.data)); stream.onerror = () => appendDebug('event stream disconnected', 'error'); } catch (error) { appendDebug(error.message, 'error'); }
const sidebar = document.querySelector('.side.right');
const contextPane = document.createElement('div'); contextPane.className = 'context-pane'; contextPane.innerHTML = '<div class="context-title">Select a node to inspect context</div>';
const sidebarTabs = document.createElement('div'); sidebarTabs.className = 'sidebar-tabs';
const sidebarBody = document.createElement('div'); sidebarBody.className = 'sidebar-body';
const inspectorPane = document.createElement('div'); inspectorPane.className = 'sidebar-pane active'; inspectorPane.append(inspector);
const sidebarDebug = document.createElement('div'); sidebarDebug.className = 'sidebar-pane'; sidebarDebug.append(debugPanel);
sidebar.replaceChildren(sidebarTabs, sidebarBody); sidebarBody.append(inspectorPane, sidebarDebug, contextPane);
for (const [label, pane] of [['Inspector', inspectorPane], ['Debug', sidebarDebug], ['Context', contextPane]]) { const button = document.createElement('button'); button.textContent = label; button.onclick = () => { for (const child of sidebarBody.children) child.classList.remove('active'); pane.classList.add('active'); for (const child of sidebarTabs.children) child.classList.remove('active'); button.classList.add('active'); if (label === 'Context') loadContext(); }; sidebarTabs.append(button); if (label === 'Inspector') button.classList.add('active'); }
debugPanel.classList.remove('debug-panel'); debugPanel.style.display = 'block'; debugPanel.style.position = 'static'; debugPanel.style.height = '100%'; debugPanel.style.boxShadow = 'none';
const sidebarStyle = document.createElement('style'); sidebarStyle.textContent = '.right{padding:0!important;display:flex;flex-direction:column}.sidebar-tabs{display:flex;border-bottom:1px solid var(--line);background:#111821}.sidebar-tabs button{flex:1;border:0;border-radius:0;color:var(--muted)}.sidebar-tabs button.active{color:var(--text);border-bottom:2px solid var(--accent)}.sidebar-body{min-height:0;flex:1;overflow:auto}.sidebar-pane{display:none;height:100%;padding:14px;overflow:auto}.sidebar-pane.active{display:block}.sidebar-debug{padding:0}.sidebar-debug .debug-head{position:sticky;top:0}.sidebar-debug .debug-panel pre{height:calc(100% - 38px)}.context-pane pre{white-space:pre-wrap;color:#b9d5eb;font:12px ui-monospace,monospace}.context-title{color:var(--muted);margin-bottom:10px}'; document.head.append(sidebarStyle);
async function loadContext() { const id = state.selectedId; if (!id) { contextPane.innerHTML = '<div class="context-title">Select a node to inspect context</div>'; return; } try { const value = await (await fetch('/api/editor/context/'+encodeURIComponent(id))).json(); contextPane.innerHTML = '<div class="context-title">Node / flow / global context</div><pre>'+escapeHtml(JSON.stringify(value, null, 2))+'</pre>'; } catch (error) { contextPane.textContent = error.message; } }
function escapeHtml(value) { return String(value).replace(/[&<>"']/g, char => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#39;' }[char])); }
let contextSelection = null; setInterval(() => { if (contextSelection !== state.selectedId) { contextSelection = state.selectedId; loadContext(); } }, 400);
const legacyWireButton = document.getElementById('wire'); if (legacyWireButton) { legacyWireButton.style.display = 'none'; legacyWireButton.onclick = null; }
if (debugButton) debugButton.style.display = 'none';
document.addEventListener('keydown', event => { if (event.key === 'w' || event.key === 'W') { event.preventDefault(); event.stopImmediatePropagation(); } }, true);
document.addEventListener('keydown', event => { if (event.key !== 'Delete') return; if (state.selectedWire) { event.preventDefault(); event.stopImmediatePropagation(); saveHistory(); const parts = state.selectedWire.split('>'), from = state.flow.flows.find(node => node.id === parts[0]), toId = parts[1], output = parts[2]; if (from) { if (output === 'link') from.links = (from.links || []).filter(id => id !== toId); else if (from.wires && from.wires[Number(output)]) from.wires[Number(output)] = from.wires[Number(output)].filter(id => id !== toId); } state.selectedWire = null; renderNodes(); setStatus('Wire deleted'); return; } const chosen = selectedNodes(); if (!chosen.length) return; event.preventDefault(); event.stopImmediatePropagation(); saveHistory(); const selectedSet = new Set(chosen); state.flow.flows = state.flow.flows.filter(item => !selectedSet.has(item)); state.selectedId = null; state.selectedIds.clear(); renderInspector(); renderNodes(); updateMetrics(); setStatus(chosen.length === 1 ? 'Node deleted' : chosen.length+' nodes deleted'); }, true);
const contrastStyle = document.createElement('style'); contrastStyle.textContent = ':root{--text:#f2f6fb;--muted:#b5c3d1}.side{color:var(--text)}.field label,.empty,.metrics{color:#b5c3d1}'; document.head.append(contrastStyle);
function renderPalette() {
    const query = document.getElementById('search').value.toLowerCase(); palette.replaceChildren();
    const groups = new Map(), hidden = new Set(['tab', 'helper', '__subflow', '__subflow-output']);
    for (const definition of state.types) { if (hidden.has(definition.type) || (query && !definition.type.toLowerCase().includes(query))) continue; const category = definition.category || (definition.type === 'inject' || definition.type === 'catch' || definition.type === 'status' ? 'Input' : definition.type === 'debug' || definition.type === 'link out' ? 'Output' : 'Function'); if (!groups.has(category)) groups.set(category, []); groups.get(category).push(definition); }
    for (const [category, definitions] of groups) { const section = document.createElement('section'); section.className = 'palette-group'; const heading = document.createElement('h4'); heading.textContent = category; section.append(heading); for (const definition of definitions) { const button = document.createElement('button'); button.className = 'palette-node'; button.style.setProperty('--node-color', definition.color || '#52708b'); const icon = document.createElement('span'); icon.className = 'palette-icon'; icon.textContent = definition.icon ? '◆' : '●'; const name = document.createElement('strong'); name.textContent = definition.type; const ports = document.createElement('small'); ports.textContent = (definition.inputs == null ? '1' : definition.inputs)+' in · '+(definition.outputs == null ? '1' : definition.outputs)+' out'; button.append(icon, name, ports); button.onclick = () => { saveHistory(); const node = { id: 'ui-'+Math.random().toString(36).slice(2, 9), type: definition.type, x: 120, y: 120, wires: [[]], ...(definition.editor && definition.editor.defaults || {}) }; if (activeTab()) node.z = activeTab().id; state.flow.flows.push(node); selectNode(node); renderNodes(); updateMetrics(); }; section.append(button); } palette.append(section); }
}
const paletteStyle = document.createElement('style'); paletteStyle.textContent = '.palette-group{margin:0 0 16px}.palette-group h4{margin:14px 0 7px;color:#9fb2c5;font-size:10px;letter-spacing:1.2px;text-transform:uppercase}.palette-node{position:relative;display:grid!important;grid-template-columns:22px 1fr;grid-template-rows:1fr 1fr;column-gap:7px;width:100%;padding:8px 9px!important;text-align:left;border:1px solid #2b3c4f;border-left:3px solid var(--node-color);background:#182330;border-radius:6px}.palette-node:hover{background:#223346;border-color:var(--node-color)}.palette-node strong{font-size:12px;line-height:15px}.palette-node small{grid-column:2;color:#8295a8;font-size:10px}.palette-icon{grid-row:1 / span 2;align-self:center;color:var(--node-color);font-size:12px}'; document.head.append(paletteStyle);
